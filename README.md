# Ollama Task LLM

This project uses an Ollama-based LLM to perform simple desktop tasks like:

- Telling the time
- Deleting files
- Opening YouTube

## Code in Progress

```bash
uvicorn main:app --reload
